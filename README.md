class named Account with all methods is defined in BankKata file
the corresponds Test file in BankKataTest file 
to run the test Open the Test Explorer (from the top menu, go to Test > Windows > Test Explorer).
